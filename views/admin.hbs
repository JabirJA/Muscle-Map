<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/styles/favicon.png" type="image/png">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>Admin - Manage Users</title>
</head>
<body>
    <div id="admin-panel">
    <img src="/styles/favicon.png" alt="Muscle Map Logo">
    <h1>Welcome, Admin!</h1>
    <h2>Manage Users and Their Fitness Goals</h2>

    <table id="users-table">
        <thead>
            <tr>
                <th>Username</th>
                <th>Fitness Goals</th>
                <th>Role</th>
                <th>Remove User</th>
            </tr>
        </thead>
        <tbody>
            {{#each users}}
            <tr>
                <td>{{userid}}</td>
                <td>{{fitness_goals}}</td>
                <td>{{role}}</td>
                <td>
                    <form action="/removeUser" method="POST" onsubmit="return confirm('Are you sure you want to remove this user?')">
                        <input type="hidden" name="userid" value="{{userid}}">
                        <button id="remove-button" type="submit" {{#if isAdmin}}disabled{{/if}}>Remove</button>
                    </form>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    <br>

    <button id="logout-btn" onclick="window.location.href='/login'">Logout</button>
    </div>
</body>
</html>
